<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cassiopeia.vgatebridge"
    android:versionCode="40"
    android:versionName="5.3.0">

    <uses-sdk android:minSdkVersion="21" android:targetSdkVersion="29" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- v4.7: necesarios para mover la tarea de Tailscale al fondo y volver
         a la app anterior (Maps/Spotify) tras abrir la VPN. -->
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.REORDER_TASKS" />
    <!-- v5.0.5: sin esto el RUN_COMMAND a Termux se RECHAZA en silencio y el
         rescate de Termux nunca funcionó (15/09: 2 días caído). Además hay que
         CONCEDERLO: Ajustes → Apps → VgateBridge → Permisos → permisos
         adicionales → «Run commands in Termux environment». -->
    <uses-permission android:name="com.termux.permission.RUN_COMMAND" />

    <application
        android:label="VgateBridge"
        android:allowBackup="false"
        android:supportsRtl="false"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
            android:showOnLockScreen="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- v5.1.0: pantalla de inicio propia (launcher). Se declara como HOME
             para poder SUSTITUIR al launcher de la ROM. Requisitos deliberados:
             - NO se autofija: la elige JC en Ajustes → Aplicación de inicio. El
               launcher de la ROM se queda instalado para poder volver.
             - singleTask + stateNotNeeded + excludeFromRecents: es una pantalla
               de inicio, no una app que se apila en el historial.
             - landscape: el salpicadero es apaisado. -->
        <activity
            android:name=".LauncherActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true"
            android:excludeFromRecents="true"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <service
            android:name=".BridgeService"
            android:exported="true"
            android:enabled="true" />

        <service
            android:name=".WakeService"
            android:exported="false"
            android:enabled="true" />

        <!-- v4.8: lee el consumo CAN del decodificador (TWUtil) y lo vuelca
             a /sdcard/Download/can_readings.csv para el recolector Termux. -->
        <service
            android:name=".CanSnifferService"
            android:exported="false"
            android:enabled="true" />

        <!-- v5.2.0: llave para poder leer el reproductor activo (Spotify, radio).
             Android exige un NotificationListenerService declarado y con el
             permiso "acceso a notificaciones" concedido a mano. El servicio no lee
             notificaciones ni guarda nada: es el componente que el sistema pide. -->
        <service
            android:name=".MediaListener"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.ACTION_POWER_CONNECTED" />
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
